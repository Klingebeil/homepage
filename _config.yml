baseurl: ""
url: "https://johannesklingebiel.de"

collections:
  portfolio:
    output: true
    permalink: /portfolio/:name
  series:
    output: true
    permalink: /collections/:name

defaults:
  - scope:
      path: ""
      type: "portfolio"
    values:
      layout: portfolio

markdown: kramdown

kramdown:
  parse_block_html: true

plugins:
  - jekyll-feed

# SASS Options
sass:
    sass_dir: _sass
